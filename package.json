{
  "name": "proyecto_si",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "dependencies": {
    "vuepress": "^1.2.0"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "push-dir": "^0.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "npm run build && git subtree push --prefix dist origin gh-pages:gh-pages",
	"deploy1": "push-dir --dir=dist --remote=gh-pages --branch=master"
  },
  "author": "",
  "license": "ISC"
}
